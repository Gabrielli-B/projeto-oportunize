

const{resolve} = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  migrationsPath: resolve(__dirname, 'src', 'database', 'migrations'),
  'seedrsPath':resolve(__dirname, 'src', 'database', 'seeds')
};

